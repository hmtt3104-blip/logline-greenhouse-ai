flask>=3.0.0
python-dotenv>=1.0.0
Pillow>=10.0.0
requests>=2.31.0

# Raspberry Pi camera — on Pi OS prefer: sudo apt install python3-picamera2
# Or: pip install picamera2  (requires libcamera; not available on all platforms)
# picamera2

# OpenCV (reference matching + person HOG detection): recommended on Pi OS:
#   sudo apt install python3-opencv
# Or: pip install opencv-python-headless — may be heavy on Pi 4
